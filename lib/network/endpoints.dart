
const register = 'auth/register';

const login = 'auth/login';

const logout = '/logout';

const adminLogout = 'admin_logout';

const userLogout = 'logout';

const home = 'get_all_postes';

const requests = 'get_my_requests';

const adminProfile = 'get_admin_info';

const userProfile = 'get_user_info';

const user_verified = 'addVerifiedUser';
